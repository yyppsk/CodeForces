#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
using namespace std;
string solve(int n, int m)
{
    string ans;
    
    return ans;
}
int main()
{
    
    int n, m;
    scanf("%d %d", &n, &m);
    cout << solve(n, m);
    return 0;
}